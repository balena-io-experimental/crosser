[package]
name = "crosser"
version = "0.1.0"
authors = ["majorz"]
edition = "2018"

[dependencies]
clap = "2"
anyhow = "1.0"
reqwest = { version = "0.10", features = ["blocking", "json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
